{
  "name": "ai-horrors-research-agent",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Daily research agent that finds AI-incident stories, dedupes against existing content + open PRs, scores them, and opens a PR with a draft.",
  "scripts": {
    "run": "bun run index.ts",
    "run:dry": "DRY_RUN=1 bun run index.ts",
    "smoke": "bun run smoke-draft.ts"
  },
  "dependencies": {
    "@openrouter/ai-sdk-provider": "^2.0.0",
    "ai": "^6.0.0",
    "gray-matter": "^4.0.3",
    "rss-parser": "^3.13.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  }
}
